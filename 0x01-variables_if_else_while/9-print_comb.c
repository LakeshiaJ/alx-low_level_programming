#include <stdio.h>

/**
 * main - prints all combinaion of a single digit
 * Return: Always 0
 */
int main(void)
{
	int n = 0;
	
	while 
	{
		putchar(48+n);
		if (n != 9)
		{
			putchar(',');
			putchar(' ')
		}
		n++
	return (0);

}
